# Attribution — outlook

Vendored from the **simply-outlook-mcp** community server (bundled in
`philipdalen/mcp-workspace`, base `665fba8`), with local modifications to:
`src/simply-outlook/graph-service.ts` and `src/tools/get-calendar-events.ts`
(primary-calendar-only fix).

Upstream license retained in `src/LICENSE`.
